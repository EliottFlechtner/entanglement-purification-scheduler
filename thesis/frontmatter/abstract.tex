\begin{abstract}

\TODO{check for e\_max occurences in figures or text; should be capital E}
\TODO{recheck for appendix references}
\TODO{add manually amde figures in appendix; ask for directions on where to add any}

Long-distance quantum networks need repeaters that distribute high-fidelity Bell pairs despite photon loss and operational noise. In all-photonic quantum repeaters built from \Gls{hrgs-long}, entanglement purification can be applied at various stages: during resource generation, after a single hop, or after the end-to-end pair is established. The copy count and circuit used may also differ at each stage. Choosing where to purify, how many copies to consume, and which circuit to apply is therefore a combinatorial scheduling problem. The source paper~\cite{Benchasattabuse2025Integrating} \TOFIX{weird phrasing?}{demonstrates one} by a single hand-picked example but does not solve.

This thesis represents every fixed non-adaptive purification schedule as a \Gls{dag} and evaluates it with a single bottom-up pass computing fidelity, success probability, latency, rate, and generation cost. The evaluator reproduces the source paper's three canonical fidelity benchmarks to four significant figures. On top of this model, three search tiers -- a structured baseline enumeration, an exact span-partition dynamic program, and a beam search with an added same-span pumping operation -- construct and compare candidate schedules under a shared generation-cost budget $E_{\max}$. Every returned candidate is a structurally validated schedule evaluated by the same physical model \TOFIX{is that what i mean?}{across all stages of the protocol}.

At the source paper's~\cite{Benchasattabuse2025Integrating} reference configuration, a schedule found by search at the same generation cost as the hand-picked baseline improves \TOFIX{specify output rate maybe? unclear so far what that is}{rate} by $2.5\%$. Allowing the search to spend less than that cost yields a schedule using only $60\%$ of \TOFIX{what is it?}{it} with a $52.8\%$ higher rate. The more \TOFIX{weird wording}{consequential result concerns} feasibility \TOFIX{sounds a little too AI like its not concerning X its concerning Y when nobody thought it would concern X ever}{rather than rate}: once inner-qubit error (a form of noise the source paper does not model) is introduced, \TOFIX{a little unclear}{the fixed baseline can fall below the required fidelity while a schedule re-selected by search at the same cost remains feasible}. The same feasibility advantage holds, on average, across a sample of independently randomized per-hop heterogeneous networks. \TOFIX{good, maybe a little convoluted}{Further experiments characterize how this advantage varies with noise level, timing effects, and chain length, including the minimum generation cost for which any searched schedule remains feasible as the chain grows.}

These results are existence claims within the schedule families and search limits implemented here, not proofs of global optimality: the beam search and its pumping operation retain only a bounded frontier at each stage. This means that even if a schedule is not found, it may still exist outside the explored frontier. Therefore, the absence of a schedule in the search results does not imply its infeasibility in the broader space of possible schedules.

%  so a schedule that is not found is only absent from the families that particular search explored, not necessarily infeasible in general.

\glsreset{hrgs-long}

\end{abstract}