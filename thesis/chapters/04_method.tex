\chapter{The Formal Model}
\label{chap:model}

% Sources: docs/instructions/Validated Formal Model Def.md,
%          src/hrgs_scheduler/models/network_config.py,
%          src/hrgs_scheduler/schedule/node.py,
%          Benchasattabuse2025Integrating,
%          Benchasattabuse2026Bridging

Chapter~\ref{chap:related} showed that the source paper
\cite{Benchasattabuse2025Integrating} demonstrates purification schedules that
mix generation, link-level, and end-node purification; however, this is only done through one
hand-picked example and leaves the search for a better schedule as an open
problem. Turning this into a problem that can actually be searched requires a
precise object to search over. This chapter builds that object step by step:
the given physical network configuration (Section~\ref{sec:model:network}), the state carried by
each partially-built Bell pair (Section~\ref{sec:model:state}), the schedule
itself as a graph of operations (Section~\ref{sec:model:dag}), the resources it
is allowed to consume (Section~\ref{sec:model:budget}), and finally the
optimization problem this thesis addresses (Section~\ref{sec:model:objectives}).
Section~\ref{sec:model:scope} closes the chapter by stating precisely which
schedules are excluded from this model and why.

The building blocks introduced in Chapter~\ref{chap:background} are reused
without redefinition in what follows; this chapter is only concerned with how those
blocks are assembled into a schedule and how schedules are compared.

\section{Network Configuration}
\label{sec:model:network}

% Source: NetworkConfig / HopConfig classes, Integrating §V-A,
%          Bridging §III, §VI-A

A network configuration fixes everything about the physical link that a
schedule cannot change. Stations are numbered $0,1,\dots,N$ along the chain,
so a hop is the link between two consecutive stations $i-1$ and $i$: an
$N$-hop chain has $N$ hops but $N+1$ stations. With $i=1,\dots,N$ indexing the hops, the
configuration is the tuple
\[
    \mathcal{N} = \Bigl(N,\ \{\ell_i\},\ \{\vec{b}^{(i)}\},\
    \{k_i\},\ \bigl\{(p_{\mathrm{in},i}^{X}, p_{\mathrm{in},i}^{Z})\bigr\},\
    \{\eta_i\},\ e_d,\ \gamma,\ c\Bigr),
\]
where, for each hop $i$: $\ell_i$ is its length, $\vec{b}^{(i)}$ is the
tree-code branching vector used to build its half-RGS, $k_i$ is its number of
redundant \gls{bsm} arms, $p_{\mathrm{in},i}^{X}$ and $p_{\mathrm{in},i}^{Z}$
are its inner-qubit measurement error rates, and $\eta_i$ is its photon
survival probability. The remaining three parameters are shared across the
whole chain rather than defined per hop: $e_d$ is the outer-photon
depolarizing rate, $\gamma$ is the memory dephasing rate, and $c$ is the
speed of light in the fiber.
Per-arm \gls{bsm} success is capped at 50\%, independent of $\eta_i$ or
$e_d$\TOCITE{recheck integrating para + find where that comes from}; this
cap is exactly why each hop is built from $k_i$ redundant arms rather than a
single attempt.

\TOFIX{rewrite after chap 6}{Every experiment in this thesis starts from the same configuration, the one
used throughout the source paper. Writing it as $\mathcal{N}_{\mathrm{src}}$,
it is $\mathcal{N}$ with every hop identical:
\[
    N = 10,\quad \ell_i = 2\text{ km},\quad \vec{b}^{(i)} = (16,14,1),\quad
    k_i = 18\ \ \forall i,\qquad e_d = 0.01,\quad \gamma = 0,
\]
\[
    p_{\mathrm{in},i}^{X} = p_{\mathrm{in},i}^{Z} = 0\ \ \forall i,\qquad
    c = 2\times 10^{5}\text{ km/s}.
\]
The remaining per-hop parameter, $\eta_i$, is not itself set to a fixed
number: it is computed from $\ell_i$ through a fixed fiber attenuation
coefficient, so specifying $\ell_i$ above already fixes it.
$\mathcal{N}_{\mathrm{src}}$ is the starting point for every experiment in
Chapter~\ref{chap:results}; each section there then varies one or two of its
fields (most often $e_d$, $k_i$, or $\gamma$) while holding the rest fixed at
these values, and restates which fields are varied at the start of that
section.}

\TODO{Add a table with one row per field of $\mathcal{N}$ ($N$, $\ell_i$,
$\vec{b}^{(i)}$, $k_i$, $p_{\mathrm{in},i}^{X}$, $p_{\mathrm{in},i}^{Z}$,
$\eta_i$, $e_d$, $\gamma$, $c$): its symbol, its value in
$\mathcal{N}_{\mathrm{src}}$, and, if it is ever swept away from that value,
a pointer to the section of Chapter~\ref{chap:results} that sweeps it (e.g.\
$e_d$ in Section~\ref{sec:exp:budgeted}, $k_i$ in
Section~\ref{sec:exp:excluded}, $\gamma$ in Section~\ref{sec:exp:timing}).
Check these section labels are actually the right ones once
Chapter~\ref{chap:results} is finalized.}

\section{Representing a Partially-Built Bell Pair}
\label{sec:model:state}

% Source: Validated Formal Model Def.md §3.1, schedule/state.py

At any point during its construction, a partially-built Bell pair, meaning one
that has not yet been extended all the way to the two end stations, is
represented by a state:
\[
    S = \bigl(\mathbf{e},\; s,\; t,\; t_{\mathrm{gen}},\; \kappa,\; r,\; h\bigr).
\]
The vector $\mathbf{e}$ is the error vector introduced in
Section~\ref{sec:bg:errorvec}, and $s$ is the Z side-effect parity from
Section~\ref{sec:bg:sideffects}. The remaining fields are new to the
scheduling layer:
\begin{itemize}
    \item $t$ and $t_{\mathrm{gen}}$ are the current simulated time and the
          time this particular resource was generated at, respectively. Their difference,
          $\Delta t_{\mathrm{idle}} = t - t_{\mathrm{gen}}$, is the idle time
          used by the decoherence law of Section~\ref{sec:bg:decoherence}.
    \item $\kappa \in \mathcal{K}$ is the \emph{span} of the resource, where
          $\mathcal{K} = \{\mathtt{RGSS}\} \cup \{\mathrm{Span}(a,b) : 0 \le a
          < b \le N\}$ is the set of possible spans for a given network configuration
          (defined formally in Section~\ref{sec:model:dag}): either \texttt{RGSS}
          for a resource that has not yet crossed a hop, or $\mathrm{Span}(a,b)$
          for an edge that already connects station $a$ to station $b$.
    \item $r$ counts how many purification rounds have already been applied to
          this branch.
    \item $h \in \{\mathit{pending}, \mathit{resolved}\}$ records whether the
          classical heralding signal for this resource has been received yet.
\end{itemize}
The field that matters most for what a schedule is allowed to do is $\kappa$:
two states can only be combined by a purification circuit if they carry the
\emph{same} $\kappa$. Two branches with different histories, one purified
twice and the other purified once for instance, can still be combined as long
as both currently sit at the same span. This single rule is what lets the
model express schedules that mix purification strategies across different
parts of the chain, which is exactly the kind of schedule the source paper's
Fig.\,4 example uses~\cite{Benchasattabuse2025Integrating}.

\section{Schedules as Directed Acyclic Graphs}
\label{sec:model:dag}

% Source: Validated Formal Model Def.md §4, schedule/node.py,
%          Justification of Implementation.md §5.1

A schedule is written as a pair $\Sigma = (T, \phi)$, where $T$ is a rooted
\gls{dag} and $\phi$ is a labelling function
\[
    \phi: T \to \mathcal{K}, \qquad
    \mathcal{K} = \{\mathtt{RGSS}\} \cup \{\mathrm{Span}(a,b) : 0 \le a < b \le N\},
\]
assigning to every node of $T$ the span $\kappa \in \mathcal{K}$ of the state
that node produces, using the same $\kappa$ introduced in
Section~\ref{sec:model:state}. The leaves of $T$ are always \texttt{Gen}
nodes (half-RGS generation) and the root is always a \texttt{PauliCorrect}
node at $\kappa = \mathrm{Span}(0,N)$, the finished end-to-end pair.

Between leaves and root, nodes come in several further kinds, each standing
for a distinct physical or logical action in the schedule:
\begin{itemize}
    \item \texttt{AbsaBsm}: the photonic \gls{bsm} that turns two same-hop
          half-RGSs into a single-hop edge;
    \item \texttt{Join}: entanglement swapping, stitching two adjacent spans
          into a wider one;
    \item \texttt{Purify}: a two-to-one YY, ZX, or XZ circuit
          (Section~\ref{sec:bg:circuits});
    \item \texttt{Herald}: resolving the classical heralding signal for a
          branch;
    \item \texttt{Idle}: an explicit wait, used only where the schedule's
          timing requires one.
\end{itemize}


\subsection{Legality Conditions}
\label{sec:model:legality}

A graph $T$ is a \emph{legal} schedule if and only if the following four conditions
all hold:
\begin{enumerate}
    \item[(i)] \texttt{Gen} nodes may only appear at $\kappa = \mathtt{RGSS}$;
    \item[(ii)] a \texttt{Purify} node's two children must share the same
          $\kappa$ \TOFIX{em dash}{---} the same span-matching rule stated informally in
          Section~\ref{sec:model:state}, now expressed as a structural condition
          on $T$ rather than a property of two states;
    \item[(iii)] a \texttt{Join} node's two children must share an endpoint,
          i.e.\ $(a,b)$ and $(b,c)$ for some $a<b<c$ (the two spans need not
          come from neighbouring hops, only meet at station $b$), and it then
          produces $(a,c)$;
    \item[(iv)] the root must sit at $\kappa=\mathrm{Span}(0,N)$.
\end{enumerate}
These four conditions are the complete legality specification: any graph
satisfying all four is a schedule that the physical hardware described in
Section~\ref{sec:model:network} is capable of executing.

This representation was checked against the source paper's own Fig.\,4
schedule~\cite{Benchasattabuse2025Integrating}, which builds one Bell pair
from purified individual links, a second from two purified 5-hop segments,
and a third with no purification at all,
then combines all three with a final purification round at the end nodes. It
combines branches purified to different degrees and built up in different
ways, and this is exactly the class of schedule this model can express:
allowing $\kappa$ to be an arbitrary span, and requiring \texttt{Purify} to
check only $\kappa$ and not how each branch was built, is what makes that
combination legal.
\TODO{Insert the worked $N=2$ DAG figure from
\texttt{docs/instructions/Validated Formal Model Def.md} §9 (also used in
Appendix~\ref{app:dags}) as a running example.}

\TOFIX{rewrite after chap 6}{Recheck this claim and the exact figure (four
significant figures) once Chapter~\ref{chap:results}/Section~6.2 is
finalized, the numbers and the cross-reference target may still move.
Section~\ref{sec:exp:validation} confirms this representation is not just
descriptive: evaluating the three canonical schedule shapes (raw, end-node
heralded, end-node optimistic) through the same bottom-up pass used everywhere
else in this thesis reproduces the source paper's own closed-form fidelity
numbers to four significant figures.}

\section{Resource Budget}
\label{sec:model:budget}

% Source: Validated Formal Model Def.md §5, ResourceBudget class

A schedule is only meaningful together with a budget
\[
    B = (n_{\mathrm{pur}},\ e_{\max},\ M_{\max}),
\]
where $n_{\mathrm{pur}}$ is the number of extra half-RGS copies generated per
purification round, $e_{\max}$ is the maximum number of \texttt{Gen} leaves
$T$ is allowed to contain, and $M_{\max}$ is the maximum number of branches
that may be under construction at the same time.
\TOFIX{future work}{$M_{\max}$ is defined here as part of the formal budget
but is not yet used anywhere in this thesis; fit its actual enforcement in as
a future-work extension rather than pretending it is already handled.}

A schedule $\Sigma$ is \emph{feasible} with respect to $B$ if it uses at most
$e_{\max}$ \texttt{Gen} leaves and never holds more than $M_{\max}$ branches
open at once. The resource cost $C(\Sigma)$ used throughout
Chapter~\ref{chap:results} is simply the number of \texttt{Gen} leaves in $T$,
so feasibility with respect to $e_{\max}$ is the same as $C(\Sigma) \le
e_{\max}$.

The source paper's own budget convention is $e_{\max} = 10N$: five half-RGS
copies per side, on both sides, for every hop. This convention is used as the
reference budget throughout the experiments, and Section~\ref{sec:exp:budget}
asks directly whether it remains large enough as $N$ grows.
$M_{\max}$ is part of the formal budget but, as stated in
Section~\ref{sec:model:scope}, it is not currently enforced by the search or
by the DAG validator; every reported result should be read with this in mind.
\TOFIX{future work}{Same note as above: revisit this paragraph once/if
$M_{\max}$ enforcement is actually added, this thesis currently only defines
it.}

\section{Objective Functions and the Optimization Problem}
\label{sec:model:objectives}

% Source: cost_functions.py, Validated Formal Model Def.md §6,
%         Optimizer Status.md §Shared Optimization Contract

Four quantities can be read off the finished schedule DAG by a single
bottom-up pass:
\begin{itemize}
    \item fidelity $F(\Sigma)$, the $w$ component of the error vector at the
          root;
    \item rate $R(\Sigma) = P_{\mathrm{success}} / T_{\mathrm{gen}}$,
          combining the schedule's overall success probability with its
          expected generation time under a renewal argument: if any branch
          of $\Sigma$ fails partway through, the entire schedule restarts
          from scratch rather than only retrying that branch, so
          $T_{\mathrm{gen}}$ is the expected wall-clock time per successful
          end-to-end pair once these full restarts are accounted for;
    \item resource cost $C(\Sigma)$, defined in Section~\ref{sec:model:budget};
    \item latency $L(\Sigma)$, the makespan of $T$.
\end{itemize}

The optimization problem this thesis addresses is then
\[
    \Sigma^{*}(\mathcal{N}, B) = \argmax_{\substack{\Sigma \text{ legal w.r.t. } \mathcal{N} \\ \Sigma \text{ feasible w.r.t. } B}}
    R(\Sigma;\mathcal{N}) \quad \text{s.t.} \quad F(\Sigma;\mathcal{N}) \ge F_{\min}.
\]
In other words, among every legal, feasible schedule, find the one with the highest
rate that still clears a minimum fidelity requirement. This is the objective
used in every headline result in Chapter~\ref{chap:results} unless stated
otherwise, with $F_{\min}=0.9$ matching the source paper's own threshold.

The same four cost functions support other research questions by swapping
which one is maximized and which are constrained: maximizing $F$ subject to a
rate floor asks for the best achievable quality at a given throughput;
minimizing $C$ subject to floors on $F$ and $R$ asks for the cheapest hardware
that still works. Both variants were implemented as alternative objective
presets while exploring these questions. Section~\ref{sec:exp:objectives}
shows that switching between all three presets requires no change to the
search algorithms of Chapter~\ref{chap:algorithms}: only the scoring
function and the feasibility constraints need to be swapped.

\section{Scope and Assumptions}
\label{sec:model:scope}

% Source: Optimizer Status.md §Known Limits

This model rests on three simplifying choices, stated explicitly here before any algorithm or result depends on them.

First, only \emph{non-adaptive} schedules are represented: $T$ is fixed before
execution starts and cannot branch on an intermediate measurement outcome.
A fully adaptive scheme, where the choice of the next purification round
depends on the fidelity estimate so far, would need a different formulation,
most naturally a Markov decision process. \TOCITE{standard reference for
MDP / sequential decision-making formulation, e.g. Puterman or Sutton and
Barto, not yet in references.bib}. This is left to future work
(Section~\ref{sec:conc:future}).

Second, $M_{\max}$ is part of the budget $B$ but is not enforced anywhere in
the current implementation, neither during search nor during DAG validation.
Every reported resource cost $C(\Sigma)$ is therefore a lower bound on the
number of physical qubit slots a real device would need to run that schedule,
since it does not account for how many branches must be held in memory at
once.
\TOFIX{future work}{Consistent with the notes in Section~\ref{sec:model:budget}:
$M_{\max}$ is defined but not enforced; keep this paragraph in sync if that
changes.}

Third, the error model throughout this thesis is the Pauli channel, the same
assumption made by the source paper. \TOCITE{general reference on Pauli
channels / stabilizer error models, e.g. Nielsen and Chuang, not yet in
references.bib}. Comparisons between schedules are made under this one noise
model, so a ranking that holds here is not automatically guaranteed to
transfer to a device dominated by a different error channel, for example pure
photon loss without any depolarizing component. This point is returned to in
Section~\ref{sec:disc:limitations}.

