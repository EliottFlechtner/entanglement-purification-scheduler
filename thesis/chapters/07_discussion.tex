\chapter{Discussion}
\label{chap:discussion}

\section{Design Principles}
\label{sec:disc:principles}

\TODO{Synthesise the five most citable generalizable findings from the
campaign as named rules (not just numerical observations). See
\texttt{docs/Design Principles.md} for the full source.
\textbf{Finding~1} (non-uniform allocation wins): composing hops erodes
fidelity non-uniformly, so concentrating purification rounds where it
matters most outperforms a uniform allocation at matched cost --- directly
observed in the headline comparison and Pareto analysis.
\textbf{Finding~2} (searched-family budget grows super-linearly): the
descriptive $N=10$--28 fit is $e_{\max}^{\min} \approx 1.369\cdot
N^{1.620}$, with a provisional, beam-width-sensitive crossover above
$10N$ near $N=28$. \textbf{Finding~3} (search restores feasibility under
changed physics): the paper schedule fails the fidelity floor under the
tested inner-qubit-error configurations while searched schedules remain
feasible. \textbf{Finding~4} (objective substitution is free): swapping
to fidelity maximisation or cost minimisation is a one-line
\texttt{ObjectiveConfig} change. \textbf{Finding~5} (timing shape controls
$\gamma$ sensitivity): only schedules with asymmetric-arrival combines
decohere under $\gamma$; the selected optimistic schedules are invariant
at the nominal configuration.}

\section{When Simple Schedules Are Near-Optimal}
\label{sec:disc:nearoptimal}

\TODO{Characterise the ``flat floor'' regime (low outer error, small $N$,
and sufficient budget) where the tested schedules differ little and the
engineering benefit of optimisation is modest. Anchor the claim to the
$e_d$ sweep, but do not claim global optimality from a beam-limited
frontier.}

\section{When the Scheduling Choice Matters}
\label{sec:disc:matters}

\TODO{Identify the complementary high-benefit regime: tight fidelity
constraints and physics that invalidates a fixed hand-tuned allocation.
At the nominal configuration, distinguish the current default cost-60,
rate-6195.95 result from the pinned historical cost-50, rate-6713.18
reproduction. Use the inner-qubit-error sensitivity experiment as the
strongest feasibility comparison, and present the $N=18$ excluded-move
result only as an existence check outside the primary heuristic search.}

\section{Optimality Scope and the Excluded-Move Gap}
\label{sec:disc:scope}

\TODO{State precisely that native DP/beam search includes one same-span
pumping move, but bounds it by default. Explain that a hand-constructed
comparison can still outperform the default pumping search, while
\texttt{exact\_pumping=True} may be impractical even at small useful
instances. Treat all pumping-enabled results as validated heuristic
constructions, not global optima. Cite \texttt{docs/Optimality Scope.md}.}

\section{Optimistic versus Heralded Purification}
\label{sec:disc:optimistic}

\TODO{Analyse the trade-off: optimistic (blind) execution saves one
classical round-trip ($L/c$ vs.\ $2L/c$ memory time) at the cost of
higher discard probability. Cite the \texttt{end\_optimistic} vs.\
\texttt{end\_heralded} brute-force comparison at the relevant $N$.}

\section{Limitations and Threats to Validity}
\label{sec:disc:limitations}

\TODO{Main modelling assumptions: Pauli channel (i.i.d.\ noise),
non-adaptive (open-loop) schedules, single-path routing, $M_{\max}$
not enforced. Discuss the first two carefully --- the Pauli
assumption is shared with the reference paper, so relative comparisons
are unaffected; the non-adaptive assumption is the most consequential
for real hardware. Note the pumping-reproducibility regression as an
example of how adding a new move can silently shift headline numbers
(documented, fixed, but a signal to watch for future extensions).}