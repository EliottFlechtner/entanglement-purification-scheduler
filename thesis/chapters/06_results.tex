\chapter{Experiments and Results}
\label{chap:results}

\section{Experimental Setup}
\label{sec:exp:setup}

\subsection{Parameter Ranges}
\label{sec:exp:params}

\TODO{Table of fixed and swept parameters. The nominal configuration is
$N=10$, $e_d=0.01$, $F_{\min}=0.9$, and $e_{\max}=100$. Sweeps cover
$N \in \{10,12,14,16,18,20,21,22,23,24,26,28\}$, outer error $e_d$,
resource budget, inner-qubit error, BSM-arm count, $\gamma$, and
$\tau_{\mathrm{emit}}$. Branching affects evaluated latency only when
$\tau_{\mathrm{emit}}$ is enabled; it was not an independent fidelity
sensitivity axis. Mark the settings used in each subsection.}

\subsection{Baselines}
\label{sec:exp:baselines}

\TODO{Three baselines, ordered by sophistication: (1)~raw chain;
(2)~uniform link-level purification (same circuit sequence at every
hop --- the practitioner's default); (3)~the paper's hand-chosen
\texttt{flexible\_paper} schedule (explicitly acknowledged by its
authors as unoptimised). Baseline (2) is new vs.\ prior single-point
reports; distinguish it clearly from (3) in figures.}

\subsection{Metrics}
\label{sec:exp:metrics}

\TODO{Define: fidelity $F$, rate $R = P_{\mathrm{success}} /
T_{\mathrm{gen}}$, resource cost $C$ (half-RGS copies consumed).
Note two comparison modes used throughout: matched-cost
(optimizer constrained to $C = 100$) and budget-relaxed (optimizer
free within $C \le e_{\max}$).}

\section{Physics Validation}
\label{sec:exp:validation}

\TODO{DAG evaluator reproduces Integrating paper Fig.~5 to
4 significant figures (raw/baseline/flexible at $e_d=0.01$:
0.8234/0.9168/0.9295 vs.\ $\approx$0.823/0.917/0.929). Include
a two-column table: analytical vs.\ evaluator for each schedule
type and several $e_d$ values. State explicitly that agreement
confirms the physical model, not the search quality.}

\section{Headline Comparison at the Paper's Nominal Config}
\label{sec:exp:headline}

\TODO{$N=10$, $e_d=0.01$. Compare two optimizer variants with the paper's
\texttt{flexible\_paper} baseline: matched cost ($C=100$, +2.5\% rate)
and budget-relaxed search. Under the current pumping-enabled default, the
latter returns cost 60, $F=0.9063$, and rate 6195.95. The earlier cost-50,
rate-6713.18 result is reproducible only with \texttt{enable\_pumping=False}.
Include the paper baseline, the matched-cost result, the current default,
and the pinned historical result as separately labelled comparisons.}

\section{Noise-Rate Sensitivity (ed Sweep)}
\label{sec:exp:edsweep}

\TODO{Sweep $e_d \in \{0, 0.001, \ldots, 0.010\}$ at $N=10$.
Show rate, fidelity, and improvement over the uniform link-level baseline.
State the bounded conclusion: the advantage is small in the low-noise
flat-floor regime and reaches up to about 18.2\% over that baseline in
the tested range. Do not infer that $e_d$ creates hop asymmetry; the
observed benefit is from schedule choice under accumulated end-to-end
noise. Source: \texttt{outputs/sweep\_ed\_n10/}.}

\section{Rate--Fidelity Tradeoff: Full Pareto Frontiers}
\label{sec:exp:pareto}

\TODO{Full $F$--$C$ and $R$--$C$ Pareto curves at the paper's nominal
config, extracted from the DP frontier machinery. The paper baseline
and the two headline optimizer variants sit as specific labelled
points on these curves. Highlight the ``elbow'' where additional
resources stop buying meaningful quality gains. Source:
\texttt{outputs/pareto\_frontiers/}.}

\subsection{Objective Substitution: Fidelity-Maximisation and Cost-Minimisation}
\label{sec:exp:objectives}

\TODO{Brief (one paragraph + one table) demonstration that swapping
\texttt{ObjectiveConfig} to \texttt{maximize\_fidelity\_with\_rate\_floor}
or \texttt{minimize\_cost\_with\_constraints} yields a structurally
different top schedule without touching any search code. The cost-
minimisation result has $C=60$ both with $F \ge 0.9$ alone and with
$R \ge R_{\text{paper}}$, but the fidelity-only preset can return a
same-cost, much lower-rate tie. Explain why callers must add $r_{\min}$
when rate matters. Cross-validate against the bisected minimum-budget
result reported later in this chapter. Source:
\texttt{outputs/alternative\_objectives/}.}

\section{Scalability and Regime Analysis}
\label{sec:exp:scalability}

\subsection{Scaling with Hop Count}
\label{sec:exp:scaling}

\TODO{Report the searched-family budget sweep through $N=28$, rather than
the superseded $N \le 18$ narrative. At the nominal noise point,
beam search finds a feasible candidate at $N=18$ with $e_{\max}=162$.
The network-sensitivity experiment instead changes inner-qubit error and
BSM-arm redundancy: the paper baseline becomes infeasible in both
non-ideal configurations, whereas the search finds feasible schedules
within the same cost budget. Source:
\texttt{outputs/sweep\_min\_budget\_vs\_n/} and
\texttt{outputs/sweep\_network\_sensitivity/}.}

\subsection{Excluded-Move Check at N = 18}
\label{sec:exp:excluded}

\TODO{Retain this as an optimality-scope experiment, not the main
scalability result. A separately constructed, bounded excluded-move
check finds a validated $N=18$ schedule with $F=0.928596$ at
$e_{\max}=180$. The current native pumping-enabled beam search also
finds feasible $N=18$ schedules, but neither observation proves global
optimality. Source: \texttt{outputs/excluded\_move\_n14\_n18/}.}

\subsection{Minimum-Budget Scaling Law and Hardware Noise Sensitivity}
\label{sec:exp:budget}

\TODO{Bisect the minimum $e_{\max}$ to clear $F_{\min}=0.9$ for
$N=10$--28. Report the descriptive fit
$e_{\max}^{\min} \approx 1.369 \cdot N^{1.620}$, the first observed
crossover at $N=28$ ($283 > 280$), and the required caveat that these
are beam-search family results, not mathematical lower bounds. The exact
crossover is provisional and beam-width-sensitive. Separately, show that
$\gamma$ affects schedules with asymmetric-arrival combines: heralded
end-node pumping collapses toward mixed fidelity at large $\gamma$, while
the optimistic schedules selected at the nominal configuration are
gamma-invariant. Explain that $\tau_{\mathrm{emit}}$ reduces rate for all
schedules and can reverse the relative timing advantage. Sources:
\texttt{outputs/sweep\_min\_budget\_vs\_n/},
\texttt{outputs/sweep\_gamma\_and\_tau\_emit/}.}