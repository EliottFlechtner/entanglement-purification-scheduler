\chapter{Introduction}
\label{chap:intro}

\section{Motivation}
\label{sec:intro:motivation}

\TODO{Quantum networks promise secure communication and distributed quantum
computation. Noisy Bell pairs are the central bottleneck. Purification
protocols improve fidelity at the cost of consuming additional copies. Searching
for optimal purification schedules is a combinatorial problem that has not been
fully addressed.}

\section{The Scheduling Gap}
\label{sec:intro:gap}

\TODO{Bencha et al.\ established the HRGS architecture and showed that
purification can be inserted at any stage (RGSS, link, end-node) with
optimistic (blind) execution to avoid double heralding round-trips.
Their numerical results use a single hand-chosen schedule and explicitly
note that schedule optimization remains open. This thesis aims to fill that gap.
Define the two source-paper shorthand names here, the first time both papers
are discussed, rather than in Chapter~\ref{chap:background}: introduce
\cite{Benchasattabuse2026Bridging} as \emph{the Bridging paper} and
\cite{Benchasattabuse2025Integrating} as \emph{the Integrating paper}
(both by Benchasattabuse, Hajdušek, and Van Meter), and state that every
later chapter uses these two shorthand names consistently instead of
repeating the full citations or author list.}

\section{Research Objectives and Contributions}
\label{sec:intro:contributions}

\TODO{State the four concrete contributions:
\begin{enumerate}
    \item A formal DAG-based model of the schedule space for HRGS-based
          quantum repeaters, with a bottom-up evaluator that reproduces
          closed-form benchmarks from prior work to 4 significant figures.
    \item A three-tier search framework (structured brute force,
          span-partition DP, and beam search). The pumping-enabled DP and
          beam tiers are bounded heuristics; every returned schedule is,
          however, structurally validated and evaluated by the same physical
          model. At the paper configuration, the matched-cost result improves
          rate by $2.5\%$; the current default budget-relaxed result achieves
          rate 6195.95 at cost 60, while a pinned pre-pumping reproduction
          obtains 6713.18 at cost 50.
    \item An empirical characterisation of the regimes where scheduling
          changes feasibility or quality, covering outer and inner-qubit
          noise, BSM-arm redundancy, hop count, memory decoherence
          ($\gamma$), and generation timing ($\tau_{\mathrm{emit}}$).
    \item Five named, evidence-backed design findings. In particular, within
          the searched families the minimum feasible budget over $N=10$--28
          is described by $e_{\max}^{\min} \approx 1.369\,N^{1.620}$;
          this is a descriptive fit, not an asymptotic law, and its
          crossover with $10N$ is beam-width-sensitive.
\end{enumerate}}

\section{Thesis Outline}
\label{sec:intro:outline}

\TODO{Give the roadmap in this order: state of the art and project
positioning (Chapter~2), background and notation (Chapter~3), formal
model and research problem (Chapter~4), algorithms and implementation
(Chapter~5), experiments and results (Chapter~6), discussion and
limitations (Chapter~7), and conclusion and perspectives (Chapter~8).}